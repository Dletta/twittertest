var Gun = require( 'gun' );
var gun = new Gun(  );

watcher1 = { _id: 123, otherval:{a:4}} ;  // works
watcher2 = { _id: 124, otherval:{a:{b:4}}} ; // works
watcher3 = { _id: 125, otherval:{a:{b:{c:3}}}}; // fails

let task = gun.get(`watcher`).put(watcher3);
gun.get('tasks').set(task);
